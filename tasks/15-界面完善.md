# 任务 15：界面完善

## 📋 任务目标
完善所有模块的前端界面，包括各技能页面、战斗界面、仓库界面等。

## 📁 需要创建/修改的文件

### 前端文件
```
frontend/src/
├── views/
│   ├── skills/
│   │   ├── Woodcutting.vue    # 伐木
│   │   ├── Mining.vue         # 采矿
│   │   ├── Fishing.vue        # 钓鱼
│   │   ├── Gathering.vue      # 采集
│   │   ├── Farming.vue        # 养殖
│   │   ├── Cooking.vue        # 烹饪
│   │   ├── Smithing.vue       # 锻造
│   │   └── Tailoring.vue      # 缝制
│   ├── Inventory.vue          # 仓库
│   ├── Character.vue          # 角色面板
│   └── Battle/
│       ├── ZoneSelect.vue     # 区域选择
│       ├── RoomList.vue       # 房间列表
│       └── BattleView.vue     # 战斗界面
├── components/
│   ├── common/
│   │   ├── ItemIcon.vue       # 物品图标
│   │   ├── SkillProgress.vue  # 技能进度条
│   │   └── StatBar.vue        # 属性条
│   ├── inventory/
│   │   ├── ItemSlot.vue       # 物品槽
│   │   └── ItemTooltip.vue    # 物品提示
│   └── battle/
│       ├── BattleUnit.vue     # 战斗单位
│       ├── SkillBar.vue       # 技能栏
│       ├── BattleLog.vue      # 战斗日志
│       └── LootPanel.vue      # 掉落面板
└── styles/
    ├── variables.scss         # 样式变量
    └── battle.scss            # 战斗样式
```

## 📝 详细实现要求

### 1. 技能页面通用布局
```vue
<template>
  <div class="skill-page">
    <!-- 技能信息头部 -->
    <div class="skill-header">
      <img :src="skill.icon" class="skill-icon" />
      <div class="skill-info">
        <h2>{{ skill.name }}</h2>
        <SkillProgress :level="skillProgress.level" :exp="skillProgress.experience" />
      </div>
    </div>
    
    <!-- 子模块列表 -->
    <div class="module-list">
      <div 
        v-for="module in modules" 
        :key="module.code"
        class="module-card"
        :class="{ locked: skillProgress.level < module.requiredLevel }"
      >
        <div class="module-header">
          <span class="module-name">{{ module.name }}</span>
          <span class="module-level">Lv.{{ module.requiredLevel }}</span>
        </div>
        
        <div class="module-info">
          <p>执行时间: {{ module.executionTime }}秒</p>
          <p>经验: +{{ module.expReward }}</p>
        </div>
        
        <!-- 产出预览 -->
        <div class="output-preview">
          <ItemIcon v-for="item in module.outputs" :key="item.code" :item="item" />
        </div>
        
        <!-- 消耗材料（合成类） -->
        <div v-if="module.inputs" class="input-materials">
          <ItemIcon v-for="item in module.inputs" :key="item.code" :item="item" :owned="getOwned(item.code)" />
        </div>
        
        <!-- 操作按钮 -->
        <div class="module-actions">
          <el-input-number v-model="module.count" :min="1" :max="99" size="small" />
          <el-button type="primary" @click="addToQueue(module)" :disabled="!canExecute(module)">
            添加到队列
          </el-button>
        </div>
      </div>
    </div>
  </div>
</template>
```

### 2. 仓库界面
```vue
<template>
  <div class="inventory-view">
    <!-- 过滤器 -->
    <div class="filters">
      <el-select v-model="filter.tag" placeholder="物品类型" clearable>
        <el-option v-for="tag in tags" :key="tag.code" :label="tag.name" :value="tag.code" />
      </el-select>
      <el-input v-model="filter.search" placeholder="搜索物品" clearable />
    </div>
    
    <!-- 物品网格 -->
    <div class="item-grid">
      <ItemSlot 
        v-for="item in filteredItems" 
        :key="item.id"
        :item="item"
        @click="selectItem(item)"
      />
    </div>
    
    <!-- 物品详情面板 -->
    <div class="item-detail" v-if="selectedItem">
      <h3>{{ selectedItem.name }}</h3>
      <p>{{ selectedItem.description }}</p>
      <div class="item-stats">
        <!-- 装备属性/效果等 -->
      </div>
      <div class="item-actions">
        <el-button v-if="isEquipment" @click="equipItem">装备</el-button>
        <el-button v-if="isSkillBook" @click="useSkillBook">学习技能</el-button>
      </div>
    </div>
  </div>
</template>
```

### 3. 角色面板
```vue
<template>
  <div class="character-panel">
    <!-- 角色信息 -->
    <div class="character-info">
      <h2>{{ character.name }}</h2>
      <el-tag>{{ character.type === 'main' ? '主号' : '铁人号' }}</el-tag>
    </div>
    
    <!-- 基础属性 -->
    <div class="base-stats">
      <StatRow label="力量" :value="totalStats.strength" :base="character.strength" />
      <StatRow label="敏捷" :value="totalStats.agility" :base="character.agility" />
      <StatRow label="耐力" :value="totalStats.endurance" :base="character.endurance" />
      <StatRow label="智力" :value="totalStats.intelligence" :base="character.intelligence" />
      <StatRow label="幸运" :value="totalStats.luck" :base="character.luck" />
    </div>
    
    <!-- 战斗属性 -->
    <div class="combat-stats">
      <StatRow label="攻击力" :value="combatStats.attack" />
      <StatRow label="防御力" :value="combatStats.defense" />
      <StatRow label="生命值" :value="combatStats.maxHP" />
      <StatRow label="魔法值" :value="combatStats.maxMP" />
      <StatRow label="速度" :value="combatStats.speed" />
      <StatRow label="暴击率" :value="combatStats.critRate" suffix="%" />
    </div>
    
    <!-- 装备槽位 -->
    <div class="equipment-slots">
      <EquipmentSlot v-for="slot in equipmentSlots" :key="slot" :slot="slot" :item="getEquippedItem(slot)" />
    </div>
  </div>
</template>
```

### 4. 战斗界面
```vue
<template>
  <div class="battle-view">
    <!-- 战斗信息 -->
    <div class="battle-info">
      <span>波次: {{ battleState.waveNumber }}</span>
      <span>回合: {{ battleState.roundNumber }}</span>
    </div>
    
    <!-- 敌人区域 -->
    <div class="enemy-area">
      <BattleUnit 
        v-for="enemy in battleState.enemies" 
        :key="enemy.id"
        :unit="enemy"
        type="enemy"
      />
    </div>
    
    <!-- 玩家区域 -->
    <div class="player-area">
      <BattleUnit 
        v-for="player in battleState.players" 
        :key="player.id"
        :unit="player"
        type="player"
      />
    </div>
    
    <!-- 技能栏 -->
    <SkillBar :skills="currentPlayerSkills" />
    
    <!-- 战斗日志 -->
    <BattleLog :events="battleEvents" />
  </div>
</template>
```

### 5. 战斗单位组件
```vue
<template>
  <div class="battle-unit" :class="[type, { dead: !unit.isAlive }]">
    <div class="unit-name">{{ unit.name }}</div>
    
    <!-- 血条 -->
    <div class="hp-bar">
      <div class="hp-fill" :style="{ width: hpPercent + '%' }"></div>
      <span class="hp-text">{{ unit.currentHP }}/{{ unit.maxHP }}</span>
    </div>
    
    <!-- 蓝条 -->
    <div class="mp-bar" v-if="type === 'player'">
      <div class="mp-fill" :style="{ width: mpPercent + '%' }"></div>
    </div>
    
    <!-- Buff/Debuff 图标 -->
    <div class="buff-icons">
      <div v-for="buff in unit.buffs" :key="buff.buffCode" class="buff-icon positive">
        <img :src="getBuffIcon(buff.buffCode)" />
        <span class="stacks" v-if="buff.stacks > 1">{{ buff.stacks }}</span>
      </div>
      <div v-for="debuff in unit.debuffs" :key="debuff.buffCode" class="buff-icon negative">
        <img :src="getBuffIcon(debuff.buffCode)" />
        <span class="stacks" v-if="debuff.stacks > 1">{{ debuff.stacks }}</span>
      </div>
    </div>
  </div>
</template>
```

### 6. 样式规范
```scss
// variables.scss
$primary-color: #667eea;
$success-color: #48bb78;
$danger-color: #f56565;
$warning-color: #ed8936;

$hp-color: #48bb78;
$mp-color: #4299e1;
$exp-color: #9f7aea;

$buff-positive: #48bb78;
$buff-negative: #f56565;

// 通用卡片样式
.card {
  background: #fff;
  border-radius: 8px;
  box-shadow: 0 2px 8px rgba(0,0,0,0.1);
  padding: 16px;
}

// 进度条样式
.progress-bar {
  height: 8px;
  background: #e2e8f0;
  border-radius: 4px;
  overflow: hidden;
  
  .fill {
    height: 100%;
    transition: width 0.3s ease;
  }
}
```

## ✅ 验证检查点

完成后请验证：
1. [ ] 所有技能页面正确显示
2. [ ] 仓库物品正确筛选和显示
3. [ ] 角色属性正确计算并显示
4. [ ] 装备槽位可视化正确
5. [ ] 战斗界面实时更新
6. [ ] Buff/Debuff 图标正确显示
7. [ ] 战斗日志正确记录
8. [ ] 响应式布局在不同屏幕下正常

## 🔗 依赖关系
- 依赖：所有前置任务（使用各模块的数据和接口）
- 贯穿：此任务可在各模块完成后逐步完善
